# Person B's data API base (the /context, /decision, /feeds/* endpoints)
VITE_API_BASE=http://localhost:8000

# Optional — Thesys C1 generative UI (only if we add the OpenUI layer)
# THESYS_C1_API_KEY=
